# V1-IAM-004 closure verification — 2026-08-08

## Durum geçişi (bu session, iki commit)

`HEAD` (f912e40) öncesinde görev `Blocked` durumundaydı; dosya diff'i tek adımda
`Done` yapılmıştı, bu legal bir geçiş değildi. Bağımsız denetim sonrası geçiş
yeniden, legal iki adımda tamamlandı:

1. `7526fc8` — `Blocked -> InProgress` (Blocker bölümü kaldırıldı, Assignee
   `opencode-v1-iam-004`). `python tools/task-scope/task_scope_tool.py
   --task-id V1-IAM-004` -> `{"valid": true, "metadata_errors": [], "findings": []}`, exit 0.
2. Bu commit — `InProgress -> Done`; yalnızca Status satırı değişti. `task-scope`
   sonra çalıştırıldığında "Task status is 'Done', expected 'Planned' or
   'InProgress'" çıktısı verir — araç yazma sınırı denetimini yalnızca executable
   durumda çalıştırır (Planned/InProgress iken exit 0, yukarıda kanıtlandı);
   Done durumu için kapanış denetimi `plan-audit validate` ile yapılır (0 hata, 0 uyarı).

Eski Blocker (`V0-ARC-001` Done olmadan kabul edilemez) artık geçersiz; tam
dependency zinciri kapandı: V0-ARC-001 Done, V0-ARC-002 Done
(AuthenticationService/login sözleşmesi), V0-GOV-003 Done, V1-FND-012 Done,
V1-IAM-001 Done.

## Uygulanan davranış (6cc2c18, master'da mevcut)

- `IUserStore.RecordLoginFailureAsync` tek `UPDATE ... RETURNING` ile atomik
  failed-attempt increment + lockout eşiği uygular; aktif lock varken null
  döner (`src/Modules/Identity/Authentication/IUserStore.cs`,
  `PostgresUserStore.cs`).
- `RecordLoginSuccessAsync` yalnız aktif lock yokken counter'ı sıfırlar.
- Eşitlikli okuma: residüel `locked_until <= now` kontrolü sayesinde süresi
  dolmuş lock'ta counter 1'den yeniden sayar.

## Acceptance kanıtı (taze koşu 2026-08-08)

```console
> dotnet test tests/Modules/Identity/Authentication/ALKAROS.Identity.Authentication.Tests.csproj --nologo --verbosity minimal
Başarılı! - Başarısız: 0, Başarılı: 51, Atlandı: 0, Toplam: 51, Süre: 5 s
exit=0
```

İlgili testler: `ConcurrentWrongPasswordsDoNotLoseAttemptsAndArmLockout`,
`LockoutArmsAfterMaxFailuresAndRejectsEvenTheCorrectPassword`,
`LockedOutAttemptDoesNotIncrementFailureCounter`,
`SuccessfulLoginClearsFailureCounterAndSetsLastLogin`,
`ExpiredLockRestartsFailureCountingAndReLocksOnlyAfterNewMaxFailures`.

## Tam çözüm zorunluluğu

`V1-IAM-004` scope'u `Authentication` projesiyle sınırlı; kabul koşulu "İlgili
testler exit code 0 verir" olduğu için hedefli proje koşusu yeterlidir.
Sonraki görevlerde full suite koşulur (`V1-IAM-002/003` aynı oturumda
tamamlanacaktır).

## Kalan

Yok. Handoff: V1-IAM-002, V1-IAM-003.
