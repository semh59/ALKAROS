# V1-SEC-003 closure verification – 2026-08-05

## Durum geçişi

Görev `Blocked` iken `Planned`'a, ardından `Done`'a alındı. Eski Blocker
(`V0-ARC-001` `Done` olmadan kabul edilemez) artık geçersiz:

- `V0-ARC-001` → Done
- `V0-GOV-003` → Done
- `V1-FND-004` → Done
- `V1-SEC-001` → Done

Uygulama kodu ve regression testleri tarihsel commit'lerde zaten mevcuttu
(`bcdd3c8` fix(host): remove command line database passwords; `a22ae0f`
fix(host): forward rollback argument). Bu kayıt, tamamlanan dependency zinciri ve
çalışır durumdaki .NET SDK üzerinde taze acceptance doğrulamasını kanıtlar.

## Acceptance kanıtı

Uygulama: `--db-password` argumenti kaldırıldı; parola yalnız
`ALKAROS_DB_PASSWORD` environment değişkeninden okunur. Eksik/boş değer
`StartupFailed` (exit 2) üretir. Bilinmeyen `--db-password` argümanı `default:
return null` dalıyla reddedilir; secret hiçbir çıktıya yazılmaz.

### Komut çıktısı (2026-08-05, sekret değeri `SUPERSECRET123`)

```console
$ dotnet ALKAROS.Host.dll --order-manifest missing.json --migrations-dir missing \
      --db-url postgresql://u@h:5432/d --db-password SUPERSECRET123
exit=2
secret leak in output: False
Usage: ALKAROS.Host --order-manifest <path> --migrations-dir <path> --db-url <url> [--psql <path>] [--rollback <position>]
  --order-manifest  Path to database/MigrationComposition/order.json
  --migrations-dir  Directory scanned for <NNN>-<name>.up.sql / .down.sql files
  --db-url          PostgreSQL connection URL (e.g. postgresql://user@host:5432/db)
  --psql            psql executable path (default: psql from PATH)
  --rollback        Run the rollback script of the given position instead of forward
  Database password is read from the ALKAROS_DB_PASSWORD environment variable.
```

- `--db-password` ile başlatma **non-zero exit (2)** verir → ✅
- Usage metninde `--db-password` yok; secret yalnız env'ten okunur → ✅
- Secret değeri (`SUPERSECRET123`) stdout/stderr'de **görünmedi** (leak: False) → ✅
- Env eksikken `StartupFailed` (exit 2) → ✅ (usage çıktısı)

## Test kanıtı

`ProgramArgumentTests` içindeki SEC-003 senaryoları:

- `MissingDatabasePasswordReturnsStartupFailure`
  — env yokken exit `StartupFailed` bekler.
- `CommandLinePasswordIsRejectedWithoutWritingTheSecret`
  — `--db-password <secret>` verilirse exit `StartupFailed` ve secret stderr'e
  yazılmaz.
- `RollbackArgumentIsForwardedToTheHostComposition`
  — env mevcutken geçerli akış; parola env'ten gider.

```console
$ dotnet test tests/Host/MigrationComposition/ALKAROS.Host.Tests.csproj --nologo --verbosity minimal
Başarılı! - Başarısız: 0, Başarılı: 62, Atlanan: 0, Toplam: 62, Süre: 13 s - ALKAROS.Host.Tests.dll
```

## Diğer doğrulamalar

- `python tools/plan-audit/plan_audit_tool.py validate` → Validation errors: 0, warnings: 0
- `python tools/task-scope/task_scope_tool.py --task-id V1-SEC-003 ...` → OK

## Kalan

Yok. Handoff: `V20-INS-001`.
