V1-SEC-001 secret leak scan
==========================

Acceptance evidence: "Secret degeri source, settings, database, exception,
log veya test snapshot icinde gorunmez."

1. Production source scan (src/BuildingBlocks/Security/Secrets/**, .cs/.csproj/.json):

   Scanned marker values (test seed constants):
     RESOLVE-VALUE-9f2c
     REDACTED-CLIENT-SECRET-c41d
     API-KEY-VALUE-2d1f
     CLIENT-SECRET-VALUE-7a03

   Result: 0 hits in src/ (all markers appear only as test seed constants
   inside tests/BuildingBlocks/Security/Secrets/**).

2. Redacted failure tests (tests/BuildingBlocks/Security/Secrets/Redaction/):

   - SecretValue.ToString() returns "<secret>" and never the value.
   - String interpolation of a SecretValue renders "<secret>".
   - SecretNotFoundException.ToString() does not contain the value.
   - SecretAccessDeniedException.ToString() does not contain the value.
   - SecretValue.Value access after Dispose throws ObjectDisposedException.

   All 21 tests pass (see test-run.txt).

3. Test run output: no secret marker value appears in the xUnit output
   captured in test-run.txt (verified by marker grep over the captured file).

4. Settings/database: this task introduces no settings surface and no
   database writes; V0-ARC-005 locks secrets out of the settings table
   (docs/architecture/settings-ownership.md, rule 1-2: OS vault / env vars).
