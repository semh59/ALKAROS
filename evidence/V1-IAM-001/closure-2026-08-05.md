# V1-IAM-001 closure verification – 2026-08-05

## Durum geçişi

Görev `Blocked` iken `Done`'a alındı. Eski Blocker (`V0-ARC-001` `Done`
olmadan kabul edilemez) artık geçersiz: tüm dependency zinciri kapandı.

- `V1-FND-001` → Done
- `V0-ARC-002` → Done
- `V0-ARC-001` → Done

Uygulama kodu ve testleri tarihsel commit'lerde zaten mevcuttu
(`2d0d7e7 feat(iam-001): implement user authentication with lockout and
sessions`). Bu kayıt, tamamlanan dependency zinciri üzerinde taze acceptance
doğrulamasını kanıtlar.

## Acceptance kanıtı

- Geçerli oturum açma: `LoginSuccess(UserId, DisplayName, IssuedSessionToken)`
  döner (Password + verify kararlı).
- Geçersiz/etkin olmayan kullanıcı: `LoginFailure(InvalidCredentials)` döner;
  unknown username ile wrong password arasında ayrım yapılmaz — kimlik bilgisi
  sızıntısı engellenir (`LoginResult.cs`).
- Lockout: alt eşikte `LoginFailure(LockedOut)`.
- Saklanan değerler tuzlanmış hash:
  `pbkdf2-sha256$<iterations>$<16-byte-salt>$<32-byte-hash>`; per-call tuz ile
  aynı parola her çağrıda farklı hash üretir (`PasswordHasherTests`).
- Migration: `database/migrations/V1/V1-IAM-001/005-users.{up,down}.sql`
  (ileri/geri).

## Test kanıtı

```console
$ dotnet test tests/Modules/Identity/Authentication/ALKAROS.Identity.Authentication.Tests.csproj --nologo --verbosity minimal
Başarılı! - Başarısız: 0, Başarılı: 51, Atlanan: 0, Toplam: 51, Süre: 3 s
```

## Diğer doğrulamalar

- `python tools/plan-audit/plan_audit_tool.py validate` → Validation errors: 0, warnings: 0

## Kalan

Yok. Handoff: `V1-IAM-002`, `V1-IAM-003`.
