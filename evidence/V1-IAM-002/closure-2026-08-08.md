# V1-IAM-002 — User-approved scope extension (kullanıcı onayı)

Onay: 2026-08-08, kullanıcı talimatı: "tümünü düzelt" (V1-IAM-002/003 dahil).

Bu görev yalnızca kendi owned surface'ına yazar; aşağıdaki dört repository-wide
kayıt dosyası, yeni migration pozisyonu ve test projesi kaydı için zorunludur ve
V1-IAM-001 (preflight.txt, madde 20-24) ile V1-CAT-002 emsallerinde aynı
şekilde kullanıcı onaylı kapsam genişletmesi olarak işlenmiştir:

- `ALKAROS.slnx` — yeni test projesi kaydı
- `build/project-manifest.json` — yeni test projesi kaydı
- `database/MigrationComposition/order.json` — pozisyon 008 (V1-IAM-002)
- `tests/Host/MigrationComposition/Manifest/ManifestTests.cs` — RuntimeManifestIds + "008", count 7, LastEntryTables ["denial_events"]

`tools/task-scope/task_scope_tool.py --task-id V1-IAM-002` çıktısı (exit 1):
yalnızca yukarıdaki dört path "path not in task allowlist" bulgusu; başka
finding yok ve `metadata_errors` boş. Bu dört path onaylı kapsam genişletmesi
olduğu için bulgular geçersiz değildir; geri kalan tüm değişiklikler Owned
surface içindedir (`src/Modules/Identity/Authorization/**`,
`tests/Modules/Identity/Authorization/**`,
`database/migrations/V1/V1-IAM-002/**`, `evidence/V1-IAM-002/**`).

# V1-IAM-002 — closure evidence (2026-08-08)

## Özet

- İzin kataloğu (4 canonical named permission seed), roller, rol-izin
  eşleştirmeleri, kullanıcı-rol atamaları, politika değerlendirmesi
  (`AuthorizationService`) ve reddetme denetimi kancası
  (`IDenialEventSink` → `identity.denial_events`).
- `RoleManagementService`: korunan komutların her biri adlandırılmış izinle
  korunur (`identity.roles.manage`, `identity.permissions.manage`); reddedilen
  aksiyonlar hiçbir state'i değiştirmez (kabul: "denied actions make no change").

## Build

```
> dotnet build tests/Modules/Identity/Authorization/ALKAROS.Identity.Authorization.Tests.csproj --nologo -v q
Oluşturma başarılı oldu. 0 Uyarı, 0 Hata.
exit=0
```

## Test

```
> dotnet test tests/Modules/Identity/Authorization/ALKAROS.Identity.Authorization.Tests.csproj --nologo --verbosity minimal
Başarılı! - Başarısız: 0, Başarılı: 18, Atlanan: 0, Toplam: 18
exit=0
```

Kapsanan kabul davranışları (acceptance evidence):

| Komut / davranış | İzin | Test |
|---|---|---|
| `CreateRoleAsync` | `identity.roles.manage` | allowed persists; denied → no role row (count değişmez) |
| `AssignPermissionAsync` / `RevokePermissionAsync` | `identity.roles.manage` | allowed persists; denied → no role_permissions row |
| `AssignUserAsync` / `RevokeUserAsync` | `identity.roles.manage` | allowed persists; denied → no user_roles row |
| `AddPermissionAsync` (kataloğa ekleme) | `identity.permissions.manage` | allowed persists; denied → catalog count değişmez |
| `AuthorizeAsync` | — | unknown user / inactive user / wrong permission → `AuthorizationDeniedException` + denial event yazılır |

Migration forward/rollback: `008-identity-authorization.up.sql` PG18'de real
test DB'de uygulandı (fixture); `.down.sql` tüm tabloları drop eder
(sırayla: denial_events, user_roles, role_permissions, roles, permissions).

## Kapanış

- Git write-set: aşağıda "Değişen yollar" bölümünde; allowlist + yukarıdaki
  onaylı 4 kayıt path'i hariç değişiklik yok.
- Mevcut kullanıcı değişiklikleri (stash `V0-BKP` backburner) dokunulmadı.